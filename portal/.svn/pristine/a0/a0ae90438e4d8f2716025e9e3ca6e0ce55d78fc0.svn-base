/**
 * This function is used to add class to profilelink id and load skillspage into mainProfileContainer div id
 * 
 */
function careermap(){$("#mainProfileContainer").load("skillspage")}function deleteCareer(a){recordnumber=a}$("#profilelink").parent().addClass("active");var recordnumber=0;